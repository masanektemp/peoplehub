# MSNC PeopleHub — Production (Windows Server)
# Salin ke root projek sebagai .env.local (dev) atau .env.production.local (server)
# Sumber rasmi: config-projek/env.example

PORT=3000
HOSTNAME=0.0.0.0

COMPANY_NAME=MSNC Group
COMPANY_CODE=MSNC

# Database (Phase 2)
# DATABASE_URL=sqlserver://localhost:1433;database=peoplehub;user=sa;password=...

# NEXT_PUBLIC_APP_URL=https://hrms.company.local